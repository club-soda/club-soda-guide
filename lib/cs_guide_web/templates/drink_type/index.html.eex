<h2>Listing Venue type</h2>

<table class="table">
  <thead>
    <tr>
      <th>Name</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for drink_type <- @drink_type do %>
    <tr>
      <td><%= drink_type.name %></td>

      <td class="text-right">
        <span><%= link "Show", to: drink_type_path(@conn, :show, drink_type.entry_id), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Edit", to: drink_type_path(@conn, :edit, drink_type.entry_id), class: "btn btn-default btn-xs" %></span>
        <span><%= link "Delete", to: drink_type_path(@conn, :delete, drink_type.entry_id), method: :delete, data: [confirm: "Are you sure?"], class: "btn btn-danger btn-xs" %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Venue types", to: drink_type_path(@conn, :new) %></span>
