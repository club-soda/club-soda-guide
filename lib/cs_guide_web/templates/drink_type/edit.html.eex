<h2>Edit Venue type</h2>

<%= render "form.html", Map.put(assigns, :action, drink_type_path(@conn, :update, @drink_type.entry_id)) %>

<span><%= link "Back", to: drink_type_path(@conn, :index) %></span>
